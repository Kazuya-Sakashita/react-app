import React from "react";
import ReactDOM from "react-dom/client"; // 新しいAPIをインポート
import { App } from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root")); // createRootを使用
root.render(<App />);
